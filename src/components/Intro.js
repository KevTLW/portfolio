import React from 'react';
import logo from '../resources/images/logo.png';

const Intro = () => (
  <div id='intro' className='screen'>
    <section className='screen-section-full'>
      <img src={logo} alt='KevTLW' />
    </section>
  </div>
);

export default Intro;